{
  "name": "mcp-sandbox",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "18"
      }
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
